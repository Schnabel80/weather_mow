# v0.4.2 — Ladezeit, Mäh-Status & Übersetzungs-Konsistenz Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mäh-Status sichtbar machen, Ladezeit lernen, `next_mow_expected` aus Nässe+Dringlichkeit+Akku berechnen, und alle Entitäten/States durchgängig übersetzen.

**Architecture:** Neue pure-Python-Datei `charging.py` (HA-frei, wie `wetness.py`/`drying.py`) kapselt das Laderaten-Lernen. Der Coordinator erkennt Ladephasen, persistiert die gelernte Rate in einem neuen Store, und nutzt sie in `next_mow_expected`. `block_reason` bekommt einen neuen Wert `mowing_active`. Alle 25 hartkodierten Entitätsnamen wandern auf `translation_key` mit de/en-Übersetzungen; `block_reason` bekommt State-Übersetzungen.

**Tech Stack:** Python 3.14, Home Assistant Custom Integration, pytest, ruff, ty, uv.

**Referenz-Spec:** `docs/superpowers/specs/2026-06-03-v0.4.2-ladezeit-maehstatus-uebersetzung-design.md`

**Vor jeder Task gilt:** Branch ist `develop`. Nach jeder Task läuft:
`uv run --group lint ruff check custom_components/ tests/ && uv run --group format ruff format --check custom_components/ tests/ && uv run --group test pytest -q`

---

## Task 1: `charging.py` — pure-Python Laderaten-Modell

**Files:**
- Create: `custom_components/weather_mow/charging.py`
- Test: `tests/test_charging.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_charging.py
"""Tests für charging.py — Laderaten-Lernen (HA-frei)."""

from __future__ import annotations

import pytest

from custom_components.weather_mow.charging import (
    CHARGE_RATE_MAX,
    CHARGE_RATE_MIN,
    DEFAULT_CHARGE_RATE_PCT_PER_MIN,
    learn_charge_rate,
    minutes_to_target,
)


class TestLearnChargeRate:
    def test_first_measurement_below_60_no_change(self):
        rate, learned = learn_charge_rate(
            DEFAULT_CHARGE_RATE_PCT_PER_MIN, False, measured_rate=0.9, rise_pct=50.0
        )
        assert rate == pytest.approx(DEFAULT_CHARGE_RATE_PCT_PER_MIN)
        assert learned is False

    def test_first_measurement_at_60_replaces_default(self):
        # 60% Anstieg in 40 min → 1.5 %/min, ersetzt Default komplett (α=1)
        rate, learned = learn_charge_rate(
            DEFAULT_CHARGE_RATE_PCT_PER_MIN, False, measured_rate=1.5, rise_pct=60.0
        )
        assert rate == pytest.approx(1.5)
        assert learned is True

    def test_subsequent_below_20_no_change(self):
        rate, learned = learn_charge_rate(1.5, True, measured_rate=0.5, rise_pct=15.0)
        assert rate == pytest.approx(1.5)
        assert learned is True

    def test_subsequent_at_20_uses_ema(self):
        # EMA: 0.8*1.5 + 0.2*1.0 = 1.4
        rate, learned = learn_charge_rate(1.5, True, measured_rate=1.0, rise_pct=20.0)
        assert rate == pytest.approx(1.4)
        assert learned is True

    def test_clamped_to_max(self):
        rate, learned = learn_charge_rate(1.0, False, measured_rate=99.0, rise_pct=80.0)
        assert rate == pytest.approx(CHARGE_RATE_MAX)
        assert learned is True

    def test_clamped_to_min(self):
        rate, learned = learn_charge_rate(1.0, False, measured_rate=0.01, rise_pct=80.0)
        assert rate == pytest.approx(CHARGE_RATE_MIN)
        assert learned is True


class TestMinutesToTarget:
    def test_normal(self):
        assert minutes_to_target(66.0, 80.0, 1.0) == pytest.approx(14.0)

    def test_already_above_target(self):
        assert minutes_to_target(85.0, 80.0, 1.0) == 0.0

    def test_zero_rate_returns_zero(self):
        assert minutes_to_target(50.0, 80.0, 0.0) == 0.0
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --group test pytest tests/test_charging.py -q`
Expected: FAIL — `ModuleNotFoundError: charging`

- [ ] **Step 3: Write minimal implementation**

```python
# custom_components/weather_mow/charging.py
"""Laderaten-Lernen für den Mäher-Akku (Zeit-zu-SoC).

Bewusst frei von Home-Assistant-Importen, damit das Lernen eigenständig per
pytest testbar bleibt — analog zu wetness.py und drying.py.
"""

from __future__ import annotations

DEFAULT_CHARGE_RATE_PCT_PER_MIN = 1.0  # Startwert bis erste valide Messung
CHARGE_RATE_MIN = 0.2  # Plausibilitäts-Klammer unten
CHARGE_RATE_MAX = 3.0  # Plausibilitäts-Klammer oben
FIRST_LEARN_MIN_RISE_PCT = 60.0  # Erstmessung erst ab diesem SoC-Anstieg
EMA_LEARN_MIN_RISE_PCT = 20.0  # Folgemessungen ab diesem SoC-Anstieg
EMA_ALPHA = 0.2  # Glättung für Folgemessungen


def learn_charge_rate(
    current_rate: float,
    learned: bool,
    measured_rate: float,
    rise_pct: float,
) -> tuple[float, bool]:
    """Aktualisiert die gelernte Laderate aus einer Lade-Messung.

    current_rate  — bisher gespeicherte Rate (%/min)
    learned       — ob schon eine echte Messung übernommen wurde
    measured_rate — gemessene Rate dieses Ladevorgangs (%/min)
    rise_pct      — kumulierter SoC-Anstieg des Ladevorgangs (%)

    Rückgabe: (neue_rate, learned). Bei zu kleinem Anstieg unverändert.
    """
    if not learned:
        if rise_pct < FIRST_LEARN_MIN_RISE_PCT:
            return current_rate, False
        new_rate = measured_rate  # α = 1.0: Default komplett ersetzen
    else:
        if rise_pct < EMA_LEARN_MIN_RISE_PCT:
            return current_rate, True
        new_rate = (1.0 - EMA_ALPHA) * current_rate + EMA_ALPHA * measured_rate
    new_rate = max(CHARGE_RATE_MIN, min(CHARGE_RATE_MAX, new_rate))
    return new_rate, True


def minutes_to_target(battery_now: float, target_pct: float, rate: float) -> float:
    """Minuten bis der Akku target_pct erreicht. rate ≤ 0 → 0 (kein Schätzwert)."""
    if rate <= 0.0:
        return 0.0
    return max(0.0, (target_pct - battery_now) / rate)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --group test pytest tests/test_charging.py -q`
Expected: PASS (9 passed)

- [ ] **Step 5: Commit**

```bash
git add custom_components/weather_mow/charging.py tests/test_charging.py
git commit -m "feat: charging.py — gelerntes Laderaten-Modell (TDD)"
```

---

## Task 2: const.py — Charge-Store-Key

**Files:**
- Modify: `custom_components/weather_mow/const.py:206` (nach STORAGE_KEY_WETNESS)

- [ ] **Step 1: Add the storage key**

In `const.py` direkt nach der Zeile `STORAGE_KEY_WETNESS = "weather_mow_{entry_id}_wetness"` einfügen:

```python
STORAGE_KEY_CHARGE = "weather_mow_{entry_id}_charge"
```

- [ ] **Step 2: Verify import-clean**

Run: `uv run --group lint ruff check custom_components/weather_mow/const.py`
Expected: All checks passed

- [ ] **Step 3: Commit**

```bash
git add custom_components/weather_mow/const.py
git commit -m "feat: STORAGE_KEY_CHARGE Konstante"
```

---

## Task 3: Coordinator — Charge-State-Felder + Store-Init + Persistenz

**Files:**
- Modify: `custom_components/weather_mow/coordinator.py` (Import, `__init__`, `_async_setup` Store-Init, `_load_storage`, `_flush_storage`)
- Test: `tests/test_coordinator_storage.py`

- [ ] **Step 1: Write the failing test**

In `tests/test_coordinator_storage.py` in `_bare()` (nach `c._prev_rain_today = 0.0`) ergänzen:

```python
    c._charge_rate = 1.0
    c._charge_learned = False
    c._charge_start_pct = None
    c._charge_start_ts = None
    c._store_charge = AsyncMock()
```

Und eine neue Testklasse am Dateiende (vor `# ── _write_debug_csv ──`):

```python
class TestChargePersistence:
    async def test_flush_saves_charge(self):
        c = _bare()
        c._charge_rate = 1.7
        c._charge_learned = True
        await c._flush_storage()
        args = c._store_charge.async_save.call_args[0][0]
        assert args["charge_rate_pct_per_min"] == pytest.approx(1.7)
        assert args["learned"] is True

    async def test_load_restores_charge(self):
        c = _bare()
        c._store_mowing.async_load = AsyncMock(return_value=None)
        c._store_rain.async_load = AsyncMock(return_value=None)
        c._store_solar.async_load = AsyncMock(return_value=None)
        c._store_growth.async_load = AsyncMock(return_value=None)
        c._store_wetness.async_load = AsyncMock(return_value=None)
        c._store_charge.async_load = AsyncMock(
            return_value={"charge_rate_pct_per_min": 2.1, "learned": True}
        )
        await c._load_storage()
        assert c._charge_rate == pytest.approx(2.1)
        assert c._charge_learned is True

    async def test_load_charge_default_when_empty(self):
        c = _bare()
        c._charge_rate = 99.0  # soll überschrieben werden
        c._charge_learned = True
        for s in [c._store_mowing, c._store_rain, c._store_solar, c._store_growth, c._store_wetness]:
            s.async_load = AsyncMock(return_value=None)
        c._store_charge.async_load = AsyncMock(return_value=None)
        await c._load_storage()
        assert c._charge_rate == pytest.approx(1.0)
        assert c._charge_learned is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --group test pytest tests/test_coordinator_storage.py::TestChargePersistence -q`
Expected: FAIL — `_store_charge` wird nicht gespeichert/geladen bzw. AttributeError

- [ ] **Step 3: Implement**

In `coordinator.py` Import-Block (bei den anderen STORAGE_KEY-Imports, ~Zeile 98) ergänzen:
```python
    STORAGE_KEY_CHARGE,
```

In `charging.py`-Import (oben bei `.wetness`/`.drying` Imports) ergänzen:
```python
from .charging import (
    DEFAULT_CHARGE_RATE_PCT_PER_MIN,
    learn_charge_rate,
    minutes_to_target,
)
```

In `__init__` (nach `self._store_wetness: Store | None = None`, ~Zeile 175):
```python
        self._store_charge: Store | None = None
```
Und bei den Instanz-Feldern (nach `self._prev_battery_pct: float | None = None`, ~Zeile 211):
```python
        self._charge_rate: float = DEFAULT_CHARGE_RATE_PCT_PER_MIN
        self._charge_learned: bool = False
        self._charge_start_pct: float | None = None
        self._charge_start_ts: float | None = None
```

In `_async_setup` Store-Init (nach dem `self._store_wetness = Store(...)`-Block, ~Zeile 277):
```python
        self._store_charge = Store(
            self.hass,
            STORAGE_VERSION,
            STORAGE_KEY_CHARGE.format(entry_id=entry_id),
        )
```

In `_load_storage` (am Ende, nach dem wetness-Block, vor Methodenende):
```python
        if self._store_charge:
            charge_data = await self._store_charge.async_load()
            if charge_data:
                from .charging import CHARGE_RATE_MAX, CHARGE_RATE_MIN

                rate = _sf(charge_data.get("charge_rate_pct_per_min"), DEFAULT_CHARGE_RATE_PCT_PER_MIN)
                self._charge_rate = max(CHARGE_RATE_MIN, min(CHARGE_RATE_MAX, rate))
                self._charge_learned = bool(charge_data.get("learned", False))
```

In `_flush_storage` (am Ende, nach dem wetness-Save-Block):
```python
        if self._store_charge:
            await self._store_charge.async_save(
                {
                    "charge_rate_pct_per_min": self._charge_rate,
                    "learned": self._charge_learned,
                }
            )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --group test pytest tests/test_coordinator_storage.py::TestChargePersistence -q`
Expected: PASS (3 passed)

- [ ] **Step 5: Run full suite + lint**

Run: `uv run --group lint ruff check custom_components/ tests/ && uv run --group test pytest -q`
Expected: All pass

- [ ] **Step 6: Commit**

```bash
git add custom_components/weather_mow/coordinator.py tests/test_coordinator_storage.py
git commit -m "feat: Charge-Rate State + Persistenz im Coordinator"
```

---

## Task 4: Coordinator — Laderaten-Erkennung pro Update

**Files:**
- Modify: `custom_components/weather_mow/coordinator.py` (`_async_update_data`, nach Akku-Delta-Tracking ~Zeile 2026)
- Test: `tests/test_coordinator_advanced.py`

- [ ] **Step 1: Write the failing test**

Neue Testklasse in `tests/test_coordinator_advanced.py` (Imports oben: `from unittest.mock import MagicMock`; nutze vorhandene Helper-Muster der Datei):

```python
class TestChargeDetection:
    def _coord(self):
        from custom_components.weather_mow.coordinator import WeatherMowCoordinator

        c = WeatherMowCoordinator.__new__(WeatherMowCoordinator)
        c._charge_rate = 1.0
        c._charge_learned = False
        c._charge_start_pct = None
        c._charge_start_ts = None
        return c

    def test_charge_phase_start_recorded(self):
        c = self._coord()
        c._maybe_track_charge(battery_now=40.0, prev=38.0, is_mowing=False, now_ts=1000.0)
        assert c._charge_start_pct == 38.0
        assert c._charge_start_ts == 1000.0

    def test_charge_phase_learns_on_end(self):
        c = self._coord()
        # Start bei 30%
        c._maybe_track_charge(battery_now=32.0, prev=30.0, is_mowing=False, now_ts=0.0)
        # Ende: Mäher fährt los (is_mowing), Akku 95% nach 3900s = 65min, rise 65% ≥ 60
        c._maybe_track_charge(battery_now=95.0, prev=95.0, is_mowing=True, now_ts=3900.0)
        # 65% / 65min = 1.0 %/min → ersetzt Default
        assert c._charge_learned is True
        assert c._charge_rate == pytest.approx(1.0, abs=0.05)
        assert c._charge_start_ts is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --group test pytest tests/test_coordinator_advanced.py::TestChargeDetection -q`
Expected: FAIL — `_maybe_track_charge` existiert nicht

- [ ] **Step 3: Implement helper + call**

Neue Methode in `coordinator.py` (z.B. direkt vor `_update_wetness`):

```python
    def _maybe_track_charge(
        self, battery_now: float, prev: float | None, is_mowing: bool, now_ts: float
    ) -> None:
        """Erkennt Ladephasen und lernt die Laderate (%/min).

        Ladephase-Start: Akku steigt und Mäher mäht nicht.
        Ladephase-Ende: Mäher mäht ODER Akku fällt ODER Akku == 100.
        """
        rising = prev is not None and battery_now > prev
        # Start
        if (
            self._charge_start_ts is None
            and rising
            and not is_mowing
            and prev is not None
        ):
            self._charge_start_pct = prev
            self._charge_start_ts = now_ts
            return
        # Ende
        if self._charge_start_ts is not None and self._charge_start_pct is not None:
            ended = is_mowing or (prev is not None and battery_now < prev) or battery_now >= 100.0
            if ended:
                rise = battery_now - self._charge_start_pct
                minutes = (now_ts - self._charge_start_ts) / 60.0
                if minutes > 0 and rise > 0:
                    measured = rise / minutes
                    self._charge_rate, self._charge_learned = learn_charge_rate(
                        self._charge_rate, self._charge_learned, measured, rise
                    )
                self._charge_start_pct = None
                self._charge_start_ts = None
```

Aufruf in `_async_update_data` direkt nach `self._prev_battery_pct = battery_pct` (~Zeile 2026) — aber `_prev_battery_pct` VOR dem Überschreiben sichern:

```python
        prev_batt = self._prev_battery_pct
        mower_state_for_charge = self.hass.states.get(cfg.get(CONF_MOWER_ENTITY, ""))
        is_mowing_charge = (
            mower_state_for_charge is not None
            and mower_state_for_charge.state == "mowing"
        )
        self._maybe_track_charge(battery_pct, prev_batt, is_mowing_charge, now_ts)
        self._prev_battery_pct = battery_pct
```

(Die Zeile `self._prev_battery_pct = battery_pct` ersetzt die bestehende — vorher `prev_batt` ziehen.)

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --group test pytest tests/test_coordinator_advanced.py::TestChargeDetection -q`
Expected: PASS (2 passed)

- [ ] **Step 5: Full suite + lint**

Run: `uv run --group lint ruff check custom_components/ tests/ && uv run --group test pytest -q`
Expected: All pass

- [ ] **Step 6: Commit**

```bash
git add custom_components/weather_mow/coordinator.py tests/test_coordinator_advanced.py
git commit -m "feat: Laderaten-Erkennung pro Update-Zyklus"
```

---

## Task 5: Coordinator — `mowing_active` block_reason + next_mow=now

**Files:**
- Modify: `custom_components/weather_mow/coordinator.py` (`_async_update_data`, nach `next_mow_expected`-Berechnung)
- Test: `tests/test_coordinator_decision.py`

- [ ] **Step 1: Write the failing test**

Neue Tests in `tests/test_coordinator_decision.py` — nutze das vorhandene Update-Integration-Muster. Falls dort kein leichter Hebel: ein gezielter Test über `_async_update_data` mit gemocktem `hass.states`. Minimal-Variante (Helper isolieren) — neue Methode `_apply_mowing_override`:

```python
class TestMowingActiveOverride:
    def _coord(self):
        from custom_components.weather_mow.coordinator import WeatherMowCoordinator
        c = WeatherMowCoordinator.__new__(WeatherMowCoordinator)
        return c

    def test_override_when_mowing_and_not_stopping(self):
        c = self._coord()
        reason, nm = c._apply_mowing_override(
            block_reason="battery_low", stop_now=False, is_mowing=True, now_local="NOW"
        )
        assert reason == "mowing_active"
        assert nm == "NOW"

    def test_no_override_when_stop_now(self):
        c = self._coord()
        reason, nm = c._apply_mowing_override(
            block_reason="too_wet", stop_now=True, is_mowing=True, now_local="NOW"
        )
        assert reason == "too_wet"
        assert nm is None

    def test_no_override_when_not_mowing(self):
        c = self._coord()
        reason, nm = c._apply_mowing_override(
            block_reason="battery_low", stop_now=False, is_mowing=False, now_local="NOW"
        )
        assert reason == "battery_low"
        assert nm is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --group test pytest tests/test_coordinator_decision.py::TestMowingActiveOverride -q`
Expected: FAIL — `_apply_mowing_override` fehlt

- [ ] **Step 3: Implement helper + wire in**

Neue Methode in `coordinator.py`:

```python
    def _apply_mowing_override(
        self,
        block_reason: str,
        stop_now: bool,
        is_mowing: bool,
        now_local: datetime,
    ) -> tuple[str, datetime | None]:
        """Während aktivem, gewolltem Mähen: Status klarstellen.

        Gibt (block_reason, next_mow_override) zurück. next_mow_override ist
        now_local wenn überschrieben werden soll, sonst None.
        """
        if is_mowing and not stop_now:
            return "mowing_active", now_local
        return block_reason, None
```

Im `_async_update_data` NACH der `next_mow_expected`-Berechnung (nach dem if/elif/else-Block ~Zeile 2149) einfügen:

```python
        mower_state_now = self.hass.states.get(cfg.get(CONF_MOWER_ENTITY, ""))
        is_mowing_now = mower_state_now is not None and mower_state_now.state == "mowing"
        block_reason, _nm_override = self._apply_mowing_override(
            block_reason, stop_now, is_mowing_now, now_local
        )
        if _nm_override is not None:
            next_mow_expected = _nm_override
```

(Hinweis: `stop_now` muss zu diesem Zeitpunkt bereits final berechnet sein. Falls `stop_now` erst weiter unten finalisiert wird, den Override-Block dahinter verschieben — `block_reason` und `next_mow_expected` sind beide noch lokale Variablen bis zum Return-Dict.)

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --group test pytest tests/test_coordinator_decision.py::TestMowingActiveOverride -q`
Expected: PASS (3 passed)

- [ ] **Step 5: Full suite + lint**

Run: `uv run --group lint ruff check custom_components/ tests/ && uv run --group test pytest -q`
Expected: All pass

- [ ] **Step 6: Commit**

```bash
git add custom_components/weather_mow/coordinator.py tests/test_coordinator_decision.py
git commit -m "feat: block_reason mowing_active + next_mow=now während Mähen"
```

---

## Task 6: Coordinator — next_mow = max(dry, charge) + adaptive Schwelle im Forecast

**Files:**
- Modify: `custom_components/weather_mow/coordinator.py` (`_forecast_next_mow` Schwellenwahl ~Zeile 1760-1766; `next_mow_expected`-else-Zweig ~Zeile 2142)
- Test: `tests/test_coordinator_forecast.py`

- [ ] **Step 1: Write the failing test**

Neue Tests in `tests/test_coordinator_forecast.py`:

```python
class TestNextMowChargeCombination:
    def _coord(self):
        from custom_components.weather_mow.coordinator import WeatherMowCoordinator
        c = WeatherMowCoordinator.__new__(WeatherMowCoordinator)
        c._charge_rate = 1.0
        return c

    def test_charge_ready_when_battery_low(self):
        from datetime import datetime, timezone
        c = self._coord()
        now = datetime(2026, 6, 3, 14, 0, tzinfo=timezone.utc)
        # Akku 66 < 80, Rate 1%/min → +14 min
        ready = c._charge_ready_time(now, battery_pct=66.0, min_batt=80)
        assert (ready - now).total_seconds() / 60 == pytest.approx(14.0)

    def test_charge_ready_now_when_battery_ok(self):
        from datetime import datetime, timezone
        c = self._coord()
        now = datetime(2026, 6, 3, 14, 0, tzinfo=timezone.utc)
        ready = c._charge_ready_time(now, battery_pct=90.0, min_batt=80)
        assert ready == now
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --group test pytest tests/test_coordinator_forecast.py::TestNextMowChargeCombination -q`
Expected: FAIL — `_charge_ready_time` fehlt

- [ ] **Step 3: Implement charge-ready helper**

Neue Methode in `coordinator.py`:

```python
    def _charge_ready_time(
        self, now_local: datetime, battery_pct: float, min_batt: int
    ) -> datetime:
        """Zeitpunkt, ab dem der Akku min_batt erreicht hat (gelernte Rate)."""
        if battery_pct >= min_batt:
            return now_local
        mins = minutes_to_target(battery_pct, float(min_batt), self._charge_rate)
        return now_local + timedelta(minutes=mins)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --group test pytest tests/test_coordinator_forecast.py::TestNextMowChargeCombination -q`
Expected: PASS (2 passed)

- [ ] **Step 5: Wire into next_mow else-branch + adaptive threshold**

Im `_async_update_data` den `else`-Zweig (~Zeile 2142) erweitern:

```python
        else:
            dry_time = self._forecast_next_mow(
                cfg,
                now_local,
                now_utc,
                wetness_mm=self._wetness_mm,
                duration_today_h=duration_today_h,
            )
            charge_time = self._charge_ready_time(now_local, battery_pct, min_batt)
            if dry_time is None:
                next_mow_expected = charge_time if battery_pct < min_batt else None
            else:
                next_mow_expected = max(dry_time, charge_time)
```

In `_forecast_next_mow` die Schwellenwahl (~Zeile 1760-1766) ersetzen, sodass Zeitdruck pro Stunde berücksichtigt wird. Vorhandenen Block:

```python
            rain_next_3h = sum(
                precip_by_hour.get(h_utc + timedelta(hours=i), 0.0) for i in range(1, 4)
            )
            grace_active_h = rain_next_3h == 0.0
            eff_threshold_h = (
                max(0.0, mow_threshold - FORECAST_DISCOUNT_MM) if grace_active_h else mow_threshold
            )
```

ersetzen durch (adaptive Schwelle, Approach A — Zeitdruck spiegeln):

```python
            rain_next_3h = sum(
                precip_by_hour.get(h_utc + timedelta(hours=i), 0.0) for i in range(1, 4)
            )
            # Zeitdruck der jeweiligen Sim-Stunde (analog _compute_decision)
            day_end = h_local.replace(
                hour=mow_end.hour, minute=mow_end.minute, second=0, microsecond=0
            )
            target_buffer_h = float(cfg.get(CONF_TARGET_BUFFER_H, DEFAULT_TARGET_BUFFER_H))
            urgent_h = h_local >= (day_end - timedelta(hours=target_buffer_h))
            if urgent_h:
                eff_threshold_h = mow_threshold_urgent
                grace_active_h = False
            elif rain_next_3h > 0.0:
                eff_threshold_h = mow_threshold
                grace_active_h = False
            else:
                eff_threshold_h = max(0.0, mow_threshold - FORECAST_DISCOUNT_MM)
                grace_active_h = True
```

`mow_threshold_urgent` am Anfang von `_forecast_next_mow` laden (bei `mow_threshold`-Laden, ~Zeile 1687):

```python
        mow_threshold_urgent = DEFAULT_MOW_THRESHOLD_URGENT_MM
        if self.mow_threshold_urgent_entity is not None:
            val = self.mow_threshold_urgent_entity.native_value
            if val is not None:
                mow_threshold_urgent = float(val)
```

Imports prüfen: `CONF_TARGET_BUFFER_H`, `DEFAULT_TARGET_BUFFER_H`, `DEFAULT_MOW_THRESHOLD_URGENT_MM` müssen importiert sein (sind sie für `_compute_decision` bereits).

- [ ] **Step 6: Full suite + lint**

Run: `uv run --group lint ruff check custom_components/ tests/ && uv run --group test pytest -q`
Expected: All pass (bestehende Forecast-Tests dürfen nicht brechen — bei Abweichung Erwartungswerte prüfen)

- [ ] **Step 7: Commit**

```bash
git add custom_components/weather_mow/coordinator.py tests/test_coordinator_forecast.py
git commit -m "feat: next_mow = max(Trocken, Laden) + adaptive Schwelle im Forecast"
```

---

## Task 7: Debug-CSV — charge_rate Spalte

**Files:**
- Modify: `custom_components/weather_mow/coordinator.py` (`_write_debug_csv` Spaltenliste + Daten; Return-Dict)
- Test: `tests/test_coordinator_storage.py` (TestWriteDebugCsv)

- [ ] **Step 1: Write the failing test**

In `tests/test_coordinator_storage.py::TestWriteDebugCsv` dem `data`-Dict `"charge_rate_pct_per_min": 1.3` hinzufügen und prüfen, dass die Spalte im Header steht:

```python
    def test_charge_rate_column_present(self):
        c = _bare()
        import tempfile, os
        with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
            tmp = f.name
        os.unlink(tmp)
        try:
            c.hass.config.path = lambda name: tmp
            data = {k: 0 for k in [
                "wetness_mm","wetness_score","drying_mm","cond_mm","rain_delta_mm",
                "condition_slot_mm","temp_c","dew_point","vpd_c","wind_kmh","solar_factor",
                "eff_solar","priority","start_now","mow_allowed","stop_now","block_reason",
                "emergency_mow_active","raining","dew_present","brightness_ok","sun_elevation",
                "rain_last_1h_mm","rain_weighted_12h","rain_today_mm","rain_today_remaining",
                "rain_tomorrow","radiation_peak","battery_pct","duration_today_h",
                "duration_avg_3d_h","growth_mm","growth_ratio","fertilizer_active",
                "irrigation_active","next_mow_expected","charge_rate_pct_per_min"]}
            c._write_debug_csv(data)
            with open(tmp) as fh:
                header = fh.readline()
            assert "charge_rate_pct_per_min" in header
        finally:
            os.path.exists(tmp) and os.unlink(tmp)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run --group test pytest "tests/test_coordinator_storage.py::TestWriteDebugCsv::test_charge_rate_column_present" -q`
Expected: FAIL — Spalte fehlt im Header

- [ ] **Step 3: Implement**

In `_write_debug_csv` die `columns`-Liste am Ende (nach `"next_mow_expected"`) um `"charge_rate_pct_per_min"` erweitern. Im Return-Dict von `_async_update_data` (bei den anderen Debug-Werten) ergänzen:

```python
            "charge_rate_pct_per_min": round(self._charge_rate, 3),
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run --group test pytest "tests/test_coordinator_storage.py::TestWriteDebugCsv" -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add custom_components/weather_mow/coordinator.py tests/test_coordinator_storage.py
git commit -m "feat: charge_rate Spalte im Debug-CSV"
```

---

## Task 8: Übersetzung — sensor.py auf translation_key

**Files:**
- Modify: `custom_components/weather_mow/sensor.py` (13 `name=` → `translation_key=`)
- Modify: `custom_components/weather_mow/translations/de.json`, `translations/en.json`

- [ ] **Step 1: sensor.py umstellen**

In jeder `WeatherMowSensorDescription` das `name="..."` durch `translation_key="<key>"` ersetzen (key = bestehender `key`-Wert in snake_case). Mapping:

| key | translation_key |
|---|---|
| wetness_mm | wetness |
| priority | priority |
| duration_today_h | duration_today |
| duration_avg_3d_h | duration_avg_3d |
| rain_last_1h_mm | rain_last_1h |
| rain_weighted_12h | rain_weighted_12h |
| rain_today_mm | rain_today_total |
| rain_today_remaining | rain_today_remaining |
| rain_tomorrow | rain_tomorrow |
| solar_peak | solar_peak |
| dew_point | dew_point |
| block_reason | block_reason |
| growth_mm | grass_growth |

`next_mow_expected` bleibt unverändert (hat schon translation_key).

- [ ] **Step 2: de.json + en.json ergänzen**

In `de.json` und `en.json` unter `entity.sensor` die 13 Keys ergänzen, z.B. de.json:

```json
"sensor": {
  "next_mow_expected": { ...bestehend... },
  "wetness": {"name": "Nässe"},
  "priority": {"name": "Priorität"},
  "duration_today": {"name": "Mähdauer heute"},
  "duration_avg_3d": {"name": "Mähdauer Ø 3 Tage"},
  "rain_last_1h": {"name": "Regen letzte Stunde"},
  "rain_weighted_12h": {"name": "Regen gewichtet 12 h"},
  "rain_today_total": {"name": "Regen heute gesamt"},
  "rain_today_remaining": {"name": "Regen heute verbleibend"},
  "rain_tomorrow": {"name": "Regen morgen"},
  "solar_peak": {"name": "Solar-Spitze"},
  "dew_point": {"name": "Taupunkt"},
  "grass_growth": {"name": "Graswuchs"},
  "block_reason": {
    "name": "Sperrgrund",
    "state": {
      "mowing_active": "Mäht gerade",
      "mowing_allowed": "Mähen erlaubt",
      "too_wet": "Zu nass",
      "battery_low": "Akku zu niedrig",
      "waiting_for_favorable": "Wartet auf besseres Fenster",
      "daily_target_reached": "Tagesziel erreicht",
      "emergency_mow_tomorrow_rain": "Notmähen (Regen morgen)",
      "outside_time_window": "Außerhalb Mähfenster",
      "too_dark_hedgehog": "Zu dunkel (Igelschutz)",
      "too_hot": "Zu heiß",
      "disabled": "Deaktiviert"
    }
  }
}
```

en.json analog (englische Texte):

```json
  "wetness": {"name": "Wetness"},
  "priority": {"name": "Priority"},
  "duration_today": {"name": "Duration Today"},
  "duration_avg_3d": {"name": "Duration Avg 3d"},
  "rain_last_1h": {"name": "Rain Last 1h"},
  "rain_weighted_12h": {"name": "Rain Weighted 12h"},
  "rain_today_total": {"name": "Rain Today Total"},
  "rain_today_remaining": {"name": "Rain Today Remaining"},
  "rain_tomorrow": {"name": "Rain Tomorrow"},
  "solar_peak": {"name": "Solar Peak"},
  "dew_point": {"name": "Dew Point"},
  "grass_growth": {"name": "Grass Growth"},
  "block_reason": {
    "name": "Block Reason",
    "state": {
      "mowing_active": "Currently mowing",
      "mowing_allowed": "Mowing allowed",
      "too_wet": "Too wet",
      "battery_low": "Battery too low",
      "waiting_for_favorable": "Waiting for better window",
      "daily_target_reached": "Daily target reached",
      "emergency_mow_tomorrow_rain": "Emergency mow (rain tomorrow)",
      "outside_time_window": "Outside mow window",
      "too_dark_hedgehog": "Too dark (hedgehog protection)",
      "too_hot": "Too hot",
      "disabled": "Disabled"
    }
  }
```

- [ ] **Step 3: JSON-Gültigkeit prüfen**

Run: `python3 -m json.tool custom_components/weather_mow/translations/de.json > /dev/null && python3 -m json.tool custom_components/weather_mow/translations/en.json > /dev/null && echo OK`
Expected: OK

- [ ] **Step 4: Full suite + lint**

Run: `uv run --group lint ruff check custom_components/ && uv run --group test pytest -q`
Expected: All pass

- [ ] **Step 5: Commit**

```bash
git add custom_components/weather_mow/sensor.py custom_components/weather_mow/translations/
git commit -m "feat: sensor-Entitäten + block_reason-States übersetzt"
```

---

## Task 9: Übersetzung — binary_sensor.py + switch.py auf translation_key

**Files:**
- Modify: `custom_components/weather_mow/binary_sensor.py` (9 Namen)
- Modify: `custom_components/weather_mow/switch.py` (3 Namen: enabled, emergency_mow, irrigation_active)
- Modify: `translations/de.json`, `translations/en.json`

- [ ] **Step 1: binary_sensor.py umstellen**

Jedes `name="..."` → `translation_key="<key>"`. Mapping:

| Name | translation_key |
|---|---|
| Allowed | allowed |
| Start Now | start_now |
| Stop Now | stop_now |
| Emergency Mow | emergency_mow |
| Raining | raining |
| Dew Present | dew_present |
| Brightness OK | brightness_ok |
| Auto Resume Blocked | auto_resume_blocked |
| Irrigation Active | irrigation_active |

- [ ] **Step 2: switch.py umstellen**

`_attr_name = "Enabled"` → `_attr_translation_key = "enabled"`;
`"Emergency Mow"` → `"emergency_mow"`; `"Irrigation Active"` → `"irrigation_active"`.
(`debug_log` bleibt unverändert.)

- [ ] **Step 3: de.json + en.json ergänzen**

de.json `entity.binary_sensor`:
```json
"binary_sensor": {
  "allowed": {"name": "Erlaubt"},
  "start_now": {"name": "Jetzt starten"},
  "stop_now": {"name": "Jetzt stoppen"},
  "emergency_mow": {"name": "Notmähen"},
  "raining": {"name": "Regnet"},
  "dew_present": {"name": "Tau vorhanden"},
  "brightness_ok": {"name": "Helligkeit OK"},
  "auto_resume_blocked": {"name": "Auto-Fortsetzen blockiert"},
  "irrigation_active": {"name": "Bewässerung aktiv"}
}
```
de.json `entity.switch` (ergänzend zu debug_log):
```json
  "enabled": {"name": "Aktiviert"},
  "emergency_mow": {"name": "Notmähen"},
  "irrigation_active": {"name": "Bewässerung aktiv"}
```
en.json analog: Allowed, Start Now, Stop Now, Emergency Mow, Raining, Dew Present, Brightness OK, Auto Resume Blocked, Irrigation Active / Enabled, Emergency Mow, Irrigation Active.

- [ ] **Step 4: JSON + Suite**

Run: `python3 -m json.tool custom_components/weather_mow/translations/de.json > /dev/null && python3 -m json.tool custom_components/weather_mow/translations/en.json > /dev/null && uv run --group test pytest -q`
Expected: OK + all pass

- [ ] **Step 5: Commit**

```bash
git add custom_components/weather_mow/binary_sensor.py custom_components/weather_mow/switch.py custom_components/weather_mow/translations/
git commit -m "feat: binary_sensor + switch Entitäten übersetzt"
```

---

## Task 10: Übersetzungs-Vollständigkeits-Test

**Files:**
- Create: `tests/test_translations_complete.py`

- [ ] **Step 1: Write the test**

```python
"""Stellt sicher, dass jeder translation_key in de.json UND en.json existiert."""

from __future__ import annotations

import json
import re
from pathlib import Path

CC = Path("custom_components/weather_mow")
PLATFORMS = ["sensor", "binary_sensor", "switch", "number", "time", "date", "button"]
PLATFORM_DOMAIN = {
    "binary_sensor": "binary_sensor", "sensor": "sensor", "switch": "switch",
    "number": "number", "time": "time", "date": "date", "button": "button",
}


def _keys_in_source(platform: str) -> set[str]:
    text = (CC / f"{platform}.py").read_text(encoding="utf-8")
    return set(re.findall(r'translation_key\s*=\s*"([a-z0-9_]+)"', text))


def _load(name: str) -> dict:
    return json.loads((CC / "translations" / name).read_text(encoding="utf-8"))


def test_all_translation_keys_present_de_en():
    de = _load("de.json").get("entity", {})
    en = _load("en.json").get("entity", {})
    missing = []
    for platform in PLATFORMS:
        domain = PLATFORM_DOMAIN[platform]
        for key in _keys_in_source(platform):
            for lang_name, lang in (("de", de), ("en", en)):
                node = lang.get(domain, {}).get(key)
                if not node or "name" not in node:
                    missing.append(f"{lang_name}:{domain}.{key}")
    assert not missing, f"Fehlende Übersetzungen: {missing}"


def test_block_reason_states_complete_de_en():
    expected = {
        "mowing_active", "mowing_allowed", "too_wet", "battery_low",
        "waiting_for_favorable", "daily_target_reached", "emergency_mow_tomorrow_rain",
        "outside_time_window", "too_dark_hedgehog", "too_hot", "disabled",
    }
    for name in ("de.json", "en.json"):
        states = _load(name)["entity"]["sensor"]["block_reason"].get("state", {})
        assert expected <= set(states), f"{name}: fehlende States {expected - set(states)}"
```

- [ ] **Step 2: Run test**

Run: `uv run --group test pytest tests/test_translations_complete.py -q`
Expected: PASS (2 passed) — falls FAIL, fehlende Keys in Task 8/9 ergänzen

- [ ] **Step 3: Commit**

```bash
git add tests/test_translations_complete.py
git commit -m "test: Übersetzungs-Vollständigkeit (alle translation_keys de+en)"
```

---

## Task 11: Version bump + Beta-Release

**Files:**
- Modify: `custom_components/weather_mow/manifest.json`, `pyproject.toml`

- [ ] **Step 1: Version setzen**

`manifest.json`: `"version": "0.4.2b1"`. `pyproject.toml`: `version = "0.4.2b1"`.

- [ ] **Step 2: Voller Check**

Run: `uv run --group lint ruff check custom_components/ tests/ && uv run --group format ruff format --check custom_components/ tests/ && uv run --group test pytest -q`
Expected: All pass

- [ ] **Step 3: Commit + push (Commits VOR Tag pushen!)**

```bash
git add custom_components/weather_mow/manifest.json pyproject.toml
git commit -m "chore: Version bump 0.4.2b1"
git push origin develop
```

- [ ] **Step 4: Tag + Release**

```bash
git tag v0.4.2b1
git push origin v0.4.2b1
gh release create v0.4.2b1 --repo Schnabel80/weather_mow --prerelease --target develop \
  --title "v0.4.2b1 — Ladezeit, Mäh-Status & Übersetzungen" \
  --notes "Mäh-Status (mowing_active), gelernte Ladezeit in next_mow_expected, adaptive Schwelle im Forecast, durchgängige Übersetzung aller Entitäten + block_reason."
```

- [ ] **Step 5: Zipball-Version verifizieren**

```bash
cd /tmp && rm -rf wm_v042 && mkdir wm_v042 && cd wm_v042 && \
curl -sL "https://api.github.com/repos/Schnabel80/weather_mow/zipball/v0.4.2b1" -o z.zip && \
unzip -p z.zip "*/manifest.json" | grep version
```
Expected: `"version": "0.4.2b1"`

---

## Self-Review-Ergebnis

- **Spec-Abdeckung:** Abschnitt 1 → Task 5; Abschnitt 2 → Tasks 1–4, 7; Abschnitt 3 → Task 6; Abschnitt 4 → Tasks 8–10. ✓
- **Keine Platzhalter:** Alle Code-Blöcke konkret.
- **Typ-Konsistenz:** `learn_charge_rate(current_rate, learned, measured_rate, rise_pct)` und `minutes_to_target(battery_now, target_pct, rate)` einheitlich über Tasks 1, 3, 4, 6. `_charge_rate`/`_charge_learned`/`_charge_start_pct`/`_charge_start_ts` konsistent.
